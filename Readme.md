# Setup

```
$ python3 -m venv .venv
$ . .venv/bin/activate
$ pip install -r requirements.txt
```

# Run

```
$ . .venv/bin/activate
$ python app.py
```

# Test

(while app is running - do this in another tab/window)

$ . .venv/bin/activate
$ python test_app.py
